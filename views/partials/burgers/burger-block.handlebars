<li>
	{{burger_name}}

	<button class="change-sleep" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if devour}}Devour it!{{else}}WAKE UP!{{/if}}
	</button>

	{{!-- delete btn --}}
		<button class="delete-burger" data-id="{{id}}">
		DELETE BURGER
	</button>

</li>
